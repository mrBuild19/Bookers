<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	<p>Title</p>
	<%= f.text_field :title %>

	<p>Body</p>
	<%= f.text_area :body %>

	<br><%= f.submit 'Update Book' %></br>
<% end %>

<%= link_to "show", book_path(@book.id) %>
<p>|</p>
<%= link_to "Back", books_path %>